{% extends 'base.html.twig' %}

{% block title %}Projets - David Binaud{% endblock %}

{% block body %}
    <main class="page projects-page">
        <section class="portfolio-block projects-cards">
            <div class="container">
                <div class="heading">
                    <h2>PROJETS <strong>RÉCENT</strong><br></h2>
                </div>
                <div class="row">
                    {% for project in projects %}
                        <div class="col-md-6 col-lg-4">
                            <div class="card border-0"><a href="{{ path('project_show', {id: project.id}) }}"><img class="d-xl-flex card-img-top scale-on-hover" src="{{ asset(project.picUrl) }}" alt="Card Image"></a>
                                <div class="card-body">
                                    <h6><a href="{{ path('project_show', {id: project.id}) }}">{{ project.name }}</a></h6>
                                    <p class="text-muted card-text">{{ project.abstract }}</p>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>
    </main>
{% endblock %}
